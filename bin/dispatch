#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/_env.sh"
PYTHON_BIN="${SIGNADMIN_PYTHON_BIN:-$ROOT/.venv/bin/python}"

cd "$ROOT"
exec "$PYTHON_BIN" -m signadmin.dispatch
